# Demo Assets

## Video

See `demo-video-link.txt` for the demo video URL. Replace the placeholder with your
actual Loom, YouTube (unlisted), or Box link before submitting.

## Live Demo

See `live-demo-url.txt`. Update it if the app is deployed before submission.

## Screenshots

Screenshots live in `demo/screenshots/`. Name them sequentially so they display in
logical order:

| File | What it should show |
|---|---|
| `01-home-dashboard.png` | Main Streamlit view — asset risk map and ranked severity table visible together |
| `02-query-input.png` | Asset or region filter/selection in use — demonstrates the interactive controls |
| `03-result-output.png` | Generated maintenance plan panel — the Bob/watsonx.ai plain-English output |

Add further screenshots as `04-...png`, `05-...png`, etc. if they show additional
meaningful views (e.g. SHAP feature-importance breakdown, weather overlay).

**Before submitting:** confirm all three required screenshots above are present and
that each image is clear enough to read at normal screen size.
